==================================================
Using `pa_ppx` with Common Build-Systems for OCaml 
==================================================

There are a number of common build-systems for OCaml.  Here, we'll
describe how to use `pa_ppx` with two common ones: `make` and `dune`.



.. container:: trailer
